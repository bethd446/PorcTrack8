node_modules/
build/
# dist/ app inclus volontairement pour déploiement VPS statique
# dist/
coverage/
.DS_Store
graphify-out/
*.log

# Playwright artifacts (générés à chaque run)
tests/playwright-artifacts/
tests/playwright-report/data/
tests/playwright-report/trace/
tests/playwright-report/index.html
test-results/

# Variables d'environnement (NE JAMAIS COMMITTER)
.env
.env.local
.env.production
.env.development
!.env.example
!website/.env.example

# Website node_modules (build commité pour VPS sans Node)
website/node_modules/
# website/dist/ inclus volontairement — déployé depuis GitHub

# Shared
shared/*.js
shared/*.d.ts

.claude/worktrees/

# APK release artifacts — bloqués par défaut, sauf releases officielles
releases/*.aab
releases/*.apk
!releases/PorcTrack-v8.2.0.apk

# Keystore de production — NE JAMAIS COMMITTER
android/porctrack-release.jks
android/keystore.properties

# Fichier keystore base64 brut — NE JAMAIS COMMITTER
key_b64.txt
