{
  "name": "porctrack-website",
  "version": "8.2.0",
  "private": true,
  "description": "Site vitrine PorcTrack 8 — Agritech porcin",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "vite build",
    "preview": "vite preview --port 4174",
    "deploy": "npm run build && bash scripts/deploy.sh"
  },
  "devDependencies": {
    "vite": "^6.3.0"
  }
}
