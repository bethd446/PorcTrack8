node_modules/
build/
# dist/ : régénéré par CI (GH Actions), plus tracké depuis 2026-05-17
dist/
coverage/
.DS_Store
graphify-out/
*.log

# Playwright artifacts (générés à chaque run)
tests/playwright-artifacts/
tests/playwright-report/data/
tests/playwright-report/trace/
tests/playwright-report/index.html
test-results/

# Variables d'environnement (NE JAMAIS COMMITTER)
.env
.env.local
.env.production
.env.development
.env.*.bak
*.env.bak
!.env.example
!website/.env.example

# Backups manuels (peuvent contenir des secrets)
*.bak

# Website node_modules (build commité pour VPS sans Node)
website/node_modules/
# website/dist/ inclus volontairement — déployé depuis GitHub

# Sous-projet PorcTrack8/website + PorcTrack8/orchestrator (indépendants)
PorcTrack8/

# Shared
shared/*.js
shared/*.d.ts

.claude/worktrees/
# Screenshots de session (artefacts de debug/audit — lourds, non versionnés)
.claude/audits/*-screens/

# Audit screenshots / rapports temporaires d'agents (régénérables)
.tmp/
.tmp-audit/
_test_screenshots/
.test-screenshots/

# Smoke scripts ad hoc (debug local, ne pas versionner)
smoke-*.mjs

# APK release artifacts — bloqués par défaut, sauf releases officielles
releases/*.aab
releases/*.apk
!releases/PorcTrack-v8.2.0.apk

# Keystore de production — NE JAMAIS COMMITTER
android/porctrack-release.jks
android/keystore.properties

# Fichier keystore base64 brut — NE JAMAIS COMMITTER
key_b64.txt
