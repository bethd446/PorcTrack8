# AUDIT PorcTrack 8 — 2026-05-17

> Orchestrateur Claude Opus 4.7 (1M context). Prod = https://porctrack.tech.
> Branche `main`, HEAD `1082484`. 2145/2145 tests · tsc 0 · build OK 3.09s.

## Résumé exécutif

**Verdict global** : la stack technique est **saine** (TSC 0 erreur, ESLint 0 erreur / 149 warnings, vitest 2145/2145 passing, build prod OK), mais **trois bombes désamorçables immédiatement** ont été détectées : (1) une **clé API Mistral et un token VPS leakés en clair dans le bundle prod** servi sur porctrack.tech — exfiltration possible en 30 secondes via DevTools ; (2) le **workflow "loges + bandes" promis (1 bande = 1 loge M + 1 loge F) n'est PAS accessible** depuis le tab `/troupeau` que l'utilisateur emprunte naturellement — la logique métier (`repartitionPorceletsParLoge`, `listLogesEffectivesParBande`) existe mais est cloisonnée au seul `PorceletsReorgWizard` ; (3) les **headers HTTP de sécurité prod sont minimaux** (Hostinger CDN sert sans HSTS / X-Frame-Options / X-Content-Type-Options ; le `nginx.conf` du repo est inutilisé en prod).

**Trois chantiers exécutables en parallèle** :
- **A — Fix workflow loges/bandes** (~1 jour, zone forms + tab Loges/Bandes) : faire fonctionner le scénario "créer 2 loges M+F → 1 bande qui les fusionne" depuis le FAB `/troupeau`
- **B — Sécurité prod critique** (~4h, zone clés API + headers Hostinger) : rotation Mistral, migration vers Edge Function, configuration `.htaccess` Hostinger pour headers sécu
- **C — Dette technique haute valeur** (~3 jours, zone Supabase + repos) : 7 WARN Supabase (5 SECURITY DEFINER + 1 search_path + leaked password protection) + split `supabaseWrites.ts` (1728L) en repos par domaine

**Blocker non résolu** : Le mot de passe `AuditPorc2026!` est rejeté par Supabase (`invalid_credentials`) alors que le compte existe et s'est connecté il y a 3h. Phase 1 (tests E2E authentifiés) n'a pas pu être exécutée. **Action requise** : confirmer le mot de passe valide ou créer un compte test dédié.

---

## Phase 1 — Tests fonctionnels

### A. Login + accueil Today

| Étape | État | Détail |
|-------|------|--------|
| A1 Navigate /login | ✅ OK | Page chargée, formulaire propre (PorcTrack · "Reprends ton cahier d'éleveur.") |
| A1b Form scan | ✅ OK | 2 inputs (email/password) + 3 boutons (Se connecter / Mot de passe oublié / S'inscrire) |
| A2 Fill email/password | ✅ OK | Valeurs bien injectées dans les inputs Ionic |
| A2b POST /auth/v1/token | 🔴 **HTTP 400** | `{"code":"invalid_credentials","message":"Invalid login credentials"}` |
| A3 Post-login URL | 🔴 FAIL | Reste sur `/login` (auth rejetée) |
| A4 Toast erreur visible | ⏸ Non vérifié | Screenshot pris (`A2_login_filled.png`) — toast non capturé en background |

**Cause identifiée** : compte `contact@liegeoischristophe.com` (ID `bc96ddbd-c34d-46b1-b624-4a3dca181a2c`) existe en DB Supabase, `last_sign_in_at = 2026-05-17 12:05:53 UTC` (~3h avant l'audit). Le mot de passe `AuditPorc2026!` est rejeté. Hypothèses : mdp changé entre brief et exécution, rate-limit Supabase, ou typo dans la transmission.

**B → F non exécutables** : tous les parcours authentifiés redirigent vers `/login`. Bascule en audit "sans login".

### Phase 1 alt — Audit sans login (code + données + routes publiques)

#### Données réelles Christophe (Supabase, ID `bc96ddbd-…`)

| Entité | Compte téoriques (brief) | Compte réel (DB) | Écart |
|--------|--------|--------|-------|
| Truies | 17 | **17** | ✅ conforme |
| Verrats | 2 | **2** | ✅ conforme |
| Bandes | 12 | **5** | -7 (probablement reset post-V72-P4 cf. journal) |
| Loges | — | **15** | présentes |
| Porcelets individuels | — | **117** | présents |

#### ★ Bug racine "loges + bandes" — code source

L'utilisateur dit : "impossible de créer proprement les loges et les bandes pour les fusionner avec la règle : 1 bande = max 2 loges, 1 loge mâle + 1 loge femelle".

**Diagnostic final** : c'est un **problème d'accessibilité du workflow**, pas un trou de logique métier. Le code de la règle existe, mais n'est pas branché sur le parcours principal.

| Composant | État | Évidence |
|-----------|------|----------|
| `loges.repartition` colonne DB | ✅ existe | type text default 'NA' (MIXTE/MALES/FEMELLES/NA) — migration v71_p3 |
| `porcelets_individuels.loge_id` colonne DB | ✅ existe | FK loges, ON DELETE SET NULL — migration v72_porcelets_loge_id |
| `PorceletIndividuel` type TS | 🔴 **BUG** | `src/types/farm.ts:174-182` n'expose PAS `logeId` malgré présence DB |
| Helper `repartitionPorceletsParLoge` | ✅ existe | `src/features/onboarding/PorceletsReorgWizard.tsx:179` — F/M/MIXTE correct |
| Helper `listLogesEffectivesParBande` | ✅ existe | `src/services/supabaseWrites.ts:1446` — GROUP BY porcelets_individuels.loge_id |
| FAB tab `loges` ouvre `QuickAddLogeForm` | 🟠 partiel | `AnimalsV70.tsx:722` — form n'a pas de champ "répartition M/F" |
| FAB tab `bandes` ouvre `QuickAddBandeForm` | 🟠 partiel | `AnimalsV70.tsx:720` — form ne propose AUCUNE loge (`loge: ''` en dur ligne 132) |
| `QuickAddBandeFromLogeForm` (workflow 5 étapes) | ✅ existe | accessible depuis `TroupeauPorceletsView` et `PendingBandesView` |
| `selectAvailableLoges` (filtrage loges libres) | 🟠 partiel | `quickAddBandeFromLogeLogic.ts:103` — utilise un Set "1 bande = 1 loge unique", pas 2 |
| `PorceletsReorgWizard` (5 étapes propres) | ✅ existe | seul flow qui gère vraiment 1B=2L M+F |

**Conséquence UX** : l'éleveur qui clique sur le FAB "+" depuis le tab Loges ou Bandes obtient un form simple qui ne sait pas faire ce que la décision métier promet. Le workflow correct est enfermé dans un wizard d'onboarding ou la vue Porcelets — non évidents.

### Phase 1 alt — Routes publiques (Playwright)

15 routes testées sans login. Screenshots dans `/tmp/audit-2026-05-17/PUB_*.png`.

| Route | État | h1 / particularité |
|-------|------|--------------------|
| `/` | ✅ HTTP 200 | Landing (PorcTrack) |
| `/login` | ✅ HTTP 200 | "PorcTrack · Connexion · Reprends ton cahier d'éleveur." |
| `/signup` | ✅ HTTP 200 | Inscription |
| `/landing-v2` | ✅ HTTP 200 | "La précision en plein élevage." · 8 sections · vidéo autoplay OK · 0 typo `FROISSEE` |
| `/a-propos` | ✅ HTTP 200 | Page produit |
| `/privacy` | ✅ HTTP 200 | Politique |
| `/mentions-legales` | ✅ HTTP 200 | Mentions |
| `/contact` | ✅ HTTP 200 | Contact |

**Endpoints API** : `api.porctrack.tech/health` → **HTTP 404** (confirmé), `api.porctrack.tech/` → 404, `porctrack.tech/api/health` → 200 (mais renvoie index.html SPA — fall-through router, pas une vraie API). Le sous-domaine `api` semble inutilisé en prod.

**Service Worker** : `/service-worker.js` → 200 (rename récent commit `b4e0c63` OK), `/sw.js` → présumé 404 (test en cours).

### Phase 1.E — Compréhensibilité menus (bottom nav)

Source de vérité : `src/v70/components/v70/BottomNav.tsx:24-34`.

| Tab | Label | Icône | Route | Verdict éleveur africain |
|-----|-------|-------|-------|--------------------------|
| 1 | **Aujourd'hui** | Sun | `/today` | ✅ Clair |
| 2 | **Élevage** | Warehouse | `/troupeau` | ✅ Clair (décision A V75 actée) |
| 3 | **Repro** | Repeat | `/reproduction` | 🟠 Raccourci ambigu — "Reproduction" ou "Saillies" plus clair |
| 3' | Lots (engraisseur) | Layers | `/engraissement` | 🟠 Vague — "Engraissement" plus parlant |
| 4 | **Performance** | LineChart | `/performance` | 🟠 Trop technique — "Bilan" ou "Pilotage" plus clair pour un éleveur |
| 5 | **Réglages** | Settings2 | `/reglages` | ✅ Standard |

≥ 5 menus ambigus à clarifier : "Repro", "Performance", "Lots" (vs Lots aliment), label FAB `+` sans contextualisation, et l'écart entre le label "Élevage" sur le tab vs URL `/troupeau` (technique).

---

## Phase 2 — Audits techniques

### A. Pipeline QA (orchestrateur — sub-agent qa-runner bloqué CWD sandbox)

| Commande | Résultat |
|----------|----------|
| `npx tsc --noEmit` | ✅ **0 erreur** |
| `npx eslint src scripts` | ✅ **0 erreur**, **149 warnings** |
| `npx vitest run` | ✅ **178 fichiers, 2145 tests passing** (durée 26.5s) |
| `npm run build` | ✅ **OK 3.09s**, PWA 112 entries / 5918 KiB |

**Top warnings ESLint** (149 total) : 46 `react-hooks/purity` (`Date.now` impur dans render), 35 `react-refresh/only-export-components`, 35 `porctrack/no-uuid-jsx` (custom rule), 7 `react-hooks/exhaustive-deps`, 7 `@typescript-eslint/no-unused-vars`, 7 `@typescript-eslint/no-explicit-any`, 1 `react-hooks/set-state-in-effect`.

**Dette technique** (counts agrégés) :
- `@ts-ignore` / `@ts-expect-error` : 3 fichiers (11 occurrences cumulées)
- `: any` : 5 occurrences
- `TODO / FIXME / XXX / HACK` : 12 occurrences (dont ~7 vrais TODO, 5 faux positifs marqueurs T-XXX/V-XXX)

**Fichiers > 500 lignes** : 48 fichiers. Top candidats refacto : `services/supabaseWrites.ts` (1728L), `features/troupeau/TruieDetailView.tsx` (1556L), `services/alertEngine.test.ts` (1348L), `features/tables/bandes/BandeDetailView.tsx` (1302L), `services/perfKpiAnalyzer.ts` (1273L), `features/onboarding/PorceletsReorgWizard.tsx` (1264L).

**Aucun leak détecté** par grep des patterns `VITE_GAS|GEMINI_API_KEY|appendRow|googleSheets|SUPABASE_SERVICE_ROLE` dans `dist/`.

### B. Code review (sub-agent code-reviewer — 1ère tentative bloquée, 2e OK avec faux négatifs)

**Findings du sub-agent** (3 critiques + 3 mineurs) :

| # | Gravité | File:line | Issue | Spot-check |
|---|---------|-----------|-------|------------|
| 1 | BLOQUANT | `src/types/farm.ts:174-182` | `PorceletIndividuel` ne déclare pas `logeId` malgré présence DB | ✅ **Confirmé** par lecture manuelle (lignes 174-182) |
| 2 | MAJEUR | `listLogesEffectivesParBande` "non trouvé" | Sub-agent claim FAUX | 🔴 **Faux négatif** — la fonction existe à `supabaseWrites.ts:1446` et `repos/porcelets.repo.ts:241` |
| 3 | MAJEUR | `QuickAddBandeFromLogeForm.tsx:409-419` | `addBatchSource` erreur swallowed silencieuse | À vérifier |
| 4 | MINEUR | `vite.config.ts:150-152` | strip console.log en prod | ✅ **Confirmé OK** |
| 5 | INFO | `QuickAddBandeForm.tsx:131-132` | Poids défaut hardcodé `'1.4'` | Détail mineur |

**Note** : le sub-agent code-reviewer a subi un sandbox Bash restrictif (problème documenté dans `.claude/memory/blockers.md` 🟡 "Bash refusé dans certains sub-agents"). Re-dispatché avec workaround Read+Grep — sortie correcte cette fois, 1 faux négatif relevé en spot-check obligatoire (AGENT_CONTRACT.md).

### C. Sécurité (sub-agent security-reviewer — 10 findings, F-01 CRITICAL spot-checked)

> ★★★ **F-01 CRITICAL CONFIRMÉ** ★★★

**Spot-check orchestrateur** :
```
$ grep -l "TQXuKoW07nHON72h1j0cCGyhJW4Rc7Rm" dist/assets/*.js
dist/assets/MariusChatFullscreen-C_a2HyQa.js
dist/assets/index-D9ZZxbo1.js

$ grep -l "marius-secret-key-2026" dist/assets/*.js
dist/assets/MariusChatFullscreen-C_a2HyQa.js
dist/assets/index-D9ZZxbo1.js

$ grep "VITE_MISTRAL_API_KEY\|VITE_MARIUS_API_KEY" .env.local
.env.local:VITE_MARIUS_API_KEY=marius-secret-key-2026
.env.local:VITE_MISTRAL_API_KEY=TQXuKoW07nHON72h1j0cCGyhJW4Rc7Rm
```

| ID | Sévérité | Issue | Action |
|----|----------|-------|--------|
| **F-01** | **CRITICAL** | Clé Mistral `TQXuKoW…` + token VPS `marius-secret-key-2026` dans bundle prod (2 chunks JS, exfiltration DevTools en 30s) | 🔴 **Rotation immédiate Mistral** + migration `marius-chat` Edge Function + retirer `VITE_MISTRAL_API_KEY` / `VITE_MARIUS_API_KEY` |
| F-02 | HIGH | Prompt injection dans `marius-chat` via `context` non borné (`supabase/functions/marius-chat/index.ts:94-96`) | Limit 2000 chars + séparateur explicite |
| F-03 | HIGH | `send-push` Edge Function sans contrôle de portée farm — un user PORCHER peut push d'autres fermes s'il connaît leurs UUIDs | Ajouter check `is_member_with_role` avant query |
| F-04 | HIGH | `profiles` UPDATE sans policy RLS visible → risque auto-promotion role `'OWNER'` | Vérifier trigger `prevent_role_escalation` attaché en DB + créer policy UPDATE explicite |
| F-05 | MEDIUM | CSP `'unsafe-inline' 'unsafe-eval'` (`nginx.conf:95` MAIS prod n'utilise pas nginx — voir F-08) | Migrer vers nonces ; vérifier nécessité `unsafe-eval` Ionic/Stencil |
| F-06 | MEDIUM | `AuthContext.tsx:208` `signOut()` ne vide pas `kvStore` (rôle, farm_id persistent post-logout sur appareil partagé) | `await kvClear()` après `supabase.auth.signOut()` |
| F-07 | MEDIUM | `admin_logs.user_id` type `text` sans FK `auth.users` (faux audit trail possible) | `ALTER COLUMN user_id TYPE uuid + ADD FK` |
| F-08 | HIGH (nouveau) | **`nginx.conf` du repo N'EST PAS UTILISÉ en prod** — Hostinger CDN (`server: hcdn`) sert l'app sans HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy | Configurer `.htaccess` Hostinger (le seul levier disponible) ou migrer vers VPS où le nginx.conf serait servi |
| F-09 | LOW | CORS `*` sur Edge Functions `marius-chat` + `send-push` | Remplacer par `https://porctrack.tech` |
| F-10 | LOW | Validation MIME upload photo basée sur déclaration browser (`photoUpload.ts:211`) | Validation magic bytes côté Storage rules |

**OWASP top 10 (synthèse)** :

| OWASP | Statut | Preuve |
|-------|--------|--------|
| A01 Broken Access Control | WARN | `profiles` UPDATE sans policy ; `send-push` cross-farm |
| A02 Cryptographic Failures | **FAIL** | Clé Mistral dans `dist/assets/MariusChatFullscreen-C_a2HyQa.js` |
| A03 Injection | OK | ORM Supabase paramétré ; pas d'eval sur user input |
| A04 Insecure Design | WARN | `marius-chat context` non borné ; `send-push` sans scope |
| A05 Security Misconfiguration | **FAIL** | Headers prod absents (HSTS/X-Frame-Options/etc.) ; CSP `'upgrade-insecure-requests'` seul |
| A06 Vulnerable Components | OK | Dépendances récentes |
| A07 ID & Auth Failures | OK | Supabase Auth standard |
| A08 Software & Data Integrity | OK | Hash Vite + SW `cleanupOutdatedCaches` |
| A09 Logging & Monitoring | WARN | `admin_logs` typing fragile ; pas de SIEM |
| A10 SSRF | OK | Pas de `fetch(userInput)` libre |

### D. Supabase advisors (orchestrateur)

**Sécurité — 7 WARN** :
1. `tg_lots_touch_updated_at` search_path mutable
2-6. 5 SECURITY DEFINER functions exposées via `/rest/v1/rpc` : `current_user_farms`, `get_user_role`, `is_member_with_role`, `is_owner_or_admin`, `user_farms`
7. `auth_leaked_password_protection` disabled (action manuelle Dashboard requise)

**Performance** : 91 lints (cf. brief — RLS initplan, FK non-indexées, duplicate_index, unused_index, multiple_permissive_policies). Le fichier complet est trop volumineux pour être inliné (63 896 chars sauvegardé à `/Users/13mac/.claude/projects/.../mcp-supabase-get_advisors-1779030060470.txt`) — extraction détaillée dans chantier C.

---

## Phase 3 — 3 chantiers exécutables

> Chaque chantier est **indépendant** (zones de fichiers disjointes) → exécutables en parallèle par 3 sessions Claude distinctes.

### CHANTIER A — Fix workflow loges + bandes (1 jour, P0 utilisateur)

**Objectif mesurable** : depuis le compte Christophe, parcours "créer 2 loges (M1 mâle + F1 femelle) → créer 1 bande qui les utilise → déplacer porcelets M/F automatiquement" doit fonctionner en ≤ 3 minutes sans wizard caché.

**Zone fichiers (exclusif chantier A — évite conflits avec B/C)** :
- `src/v70/pages/AnimalsV70.tsx` (FAB routing tab `loges`/`bandes`)
- `src/components/forms/QuickAddLogeForm.tsx` (+ champ `repartition`)
- `src/components/forms/QuickAddBandeForm.tsx` (+ sélection 0/1/2 loges)
- `src/components/forms/quickAddBandeLogic.ts` (validation 1B=2L)
- `src/components/forms/quickAddBandeFromLogeLogic.ts` (élargir `selectAvailableLoges` au cas 2 loges)
- `src/types/farm.ts:174-182` (ajouter `logeId?: string` à `PorceletIndividuel`)
- `src/services/supabaseWrites.ts:mapPorcelet` (mapper `loge_id` → `logeId`)
- Tests Vitest associés

**Sous-tâches** :
1. Ajouter colonne `repartition` (radio MIXTE/MALES/FEMELLES/NA) dans `QuickAddLogeForm` → POST `loges.repartition`
2. Étendre `QuickAddBandeForm` avec un step "Affecter à des loges" (0, 1 ou 2 loges max, validation mutuelle "1 M + 1 F si 2 loges")
3. Ajouter `logeId?` au type `PorceletIndividuel` + mapper côté `supabaseWrites`
4. Quand bande créée avec 2 loges + répartition F1/M1, auto-call `repartitionPorceletsParLoge` sur les porcelets de la bande
5. Tests : créer 2 loges + 1 bande, vérifier que `listLogesEffectivesParBande(bandeId)` retourne bien les 2 loges avec sexes corrects
6. Smoke browser : compte Christophe peut faire le scénario sans wizard caché

**Critère de fin** :
- `npx tsc --noEmit` : 0 erreur
- `npx vitest run` : ≥ 2145 passing (pas de régression) + 5+ tests ajoutés
- Manuel : depuis `/troupeau` → tab Bandes → "+" → wizard 2 steps → 1 bande créée occupant 2 loges (visible dans BandeDetailView)
- Acceptance : `listLogesEffectivesParBande(b.id).length === 2 && sexes match`

---

### CHANTIER B — Sécurité prod critique (4h, P0 sécu — BLOQUE TOUT DÉPLOIEMENT)

**Objectif mesurable** : 0 secret dans `dist/`, headers HTTP prod alignés sur OWASP recommandations, F-01 fermé, advisor Supabase à 0 WARN sécu.

**Zone fichiers (exclusif chantier B)** :
- `.env.local` (suppression `VITE_MISTRAL_API_KEY` + `VITE_MARIUS_API_KEY`)
- `src/features/chatbot/mariusApi.ts` + `ChatbotWidget.tsx` (router via Edge Function uniquement)
- `supabase/functions/marius-chat/index.ts` (limit `context` 2000 chars + sanitization)
- `supabase/functions/send-push/index.ts` (check `is_member_with_role` + CORS restreint)
- `src/context/AuthContext.tsx:208` (`signOut` + `kvClear`)
- `public/.htaccess` ou `dist/.htaccess` (Hostinger — ajouter HSTS/X-Frame-Options/X-Content-Type-Options/Referrer-Policy/Permissions-Policy)
- Migration Supabase : `tg_lots_touch_updated_at` search_path + REVOKE EXECUTE sur 5 SECURITY DEFINER + create policy UPDATE on `profiles`
- Dashboard Supabase manuel : enable `leaked_password_protection`

**Sous-tâches priorisées** :
1. ★ **ROTATION CLÉ MISTRAL** sur https://console.mistral.ai (révoque `TQXuKoW…`)
2. Retirer `VITE_MISTRAL_API_KEY` + `VITE_MARIUS_API_KEY` du code & `.env.local` ; tous les appels Marius → Edge Function `marius-chat` (déjà en place, `MISTRAL_API_KEY` server-side)
3. Rebuild + déploiement Hostinger (confirmation user requise)
4. Vérifier post-déploiement : `grep -r "TQXuKoW\|marius-secret" dist/` = 0
5. Patch `marius-chat` : `if (context && context.length > 2000) throw 400 "context too long"` + séparateur `--- Données ferme (lecture seule) ---`
6. Patch `send-push` : avant query, `if (!await isMemberWithRole(callerId, farmId, 'OWNER'|'ADMIN')) return 403`
7. Patch `AuthContext.signOut` : `await kvClear()` post-`supabase.auth.signOut()`
8. `.htaccess` Hostinger : `Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"`, X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy stricte
9. Migration SQL : `ALTER FUNCTION tg_lots_touch_updated_at() SET search_path = public, pg_temp`
10. SECURITY DEFINER — décision archi (cf. journal V74 backlog) : recommandation = Option C (accept risk, fonctions retournent uniquement les farm_ids du caller — pas de leak)
11. Dashboard Supabase : activer `leaked_password_protection`

**Critère de fin** :
- `grep -r "TQXuKoW\|marius-secret" dist/` = 0
- `curl -I https://porctrack.tech/` retourne HSTS + X-Frame-Options + X-Content-Type-Options + Referrer-Policy + Permissions-Policy
- `mcp__supabase__get_advisors type=security` ≤ 1 WARN résiduel (les 5 SECURITY DEFINER documentés comme accepted risk)
- Tests Vitest 2145+ passing

---

### CHANTIER C — Dette technique haute valeur (3 jours, P1)

**Objectif mesurable** : `mcp__supabase__get_advisors type=performance` ≤ 5 INFO résiduelles, `supabaseWrites.ts` splitté en repos par domaine (≤ 500L par fichier), libellés bottom-nav clarifiés.

**Zone fichiers (exclusif chantier C — évite conflits A/B)** :
- `src/services/supabaseWrites.ts` (split — 1728L)
- `src/services/repos/*.ts` (nouveaux : truies, verrats, bandes, loges, porcelets, finances, alertes)
- Migrations Supabase : indexes manquants FK + déduplication policies permissives + remove unused indexes
- `src/v70/components/v70/BottomNav.tsx:24-34` (renommer "Repro" → "Reproduction", "Performance" → "Bilan" ou conserver "Pilotage" selon arbitrage)
- Tests Vitest associés

**Sous-tâches** :
1. **Supabase performance** :
   - Extraire les 91 lints du fichier `mcp-supabase-get_advisors-1779030060470.txt`
   - Créer migration `v83_perf_rls_indexes` : ajouter 19 indexes FK manquants, supprimer 7 unused_index, fusionner les multiple_permissive_policies
   - Patcher 19 RLS initplan : `auth.uid()` → `(SELECT auth.uid())`
2. **Split `supabaseWrites.ts`** (1728L → ≤ 7 fichiers de ≤ 500L) :
   - `repos/truies.repo.ts` (CRUD truies + saillies)
   - `repos/verrats.repo.ts`
   - `repos/bandes.repo.ts` (insertBatch + listLogesEffectivesParBande + addBatchSource)
   - `repos/loges.repo.ts` (déjà existant — étendre)
   - `repos/porcelets.repo.ts` (déjà existant — étendre)
   - `repos/finances.repo.ts`
   - `repos/notes.repo.ts`
   - `services/supabaseWrites.ts` devient un re-export thin (rétro-compat)
3. **UX clarté libellés** :
   - "Repro" → "Reproduction"
   - "Performance" → "Pilotage" (ou "Bilan" — arbitrage user)
   - "Lots" → "Engraissement" (visible engraisseur seul)
4. **149 ESLint warnings** :
   - 46 `react-hooks/purity` (Date.now impur) → wrapper dans `useNow()` helper (1 fichier, 30 lignes)
   - 35 `react-refresh/only-export-components` → restructurer exports
   - 7 `react-hooks/exhaustive-deps` → fix au cas par cas

**Critère de fin** :
- `mcp__supabase__get_advisors type=performance` ≤ 5 INFO résiduelles
- `wc -l src/services/supabaseWrites.ts` ≤ 200 (re-export thin)
- `wc -l src/services/repos/*.ts` chaque ≤ 500
- `npx eslint src scripts` ≤ 30 warnings (vs 149 actuels)
- Bottom nav : 5 labels validés par éleveur en test (action user externe)
- Tests Vitest 2145+ passing (0 régression)

---

## Annexe — Bugs exhaustifs par sévérité

### P0 (BLOQUANT immédiat — production en danger)

| ID | Issue | Évidence | Chantier |
|----|-------|----------|----------|
| P0-1 | Clé Mistral + token VPS leakés dans bundle prod | `grep "TQXuKoW" dist/assets/MariusChatFullscreen-*.js` retourne 2 hits | B |
| P0-2 | Workflow loges+bandes (1 bande = 1 M + 1 F) inaccessible depuis FAB principal | Code logique existe (`PorceletsReorgWizard.tsx:179`) mais non branché sur `AnimalsV70.tsx:720-722` | A |
| P0-3 | Headers HTTP sécu absents en prod (HSTS, X-Frame-Options, X-CTO, Referrer, Permissions) | `curl -I https://porctrack.tech/` — Hostinger CDN minimal | B |
| P0-4 | `profiles` UPDATE sans policy RLS visible → auto-promotion role | Aucune policy `FOR UPDATE ON profiles` trouvée dans 9 migrations auditées | B |

### P1 (Frein important — utilisateur ralenti / risque éditorial)

| ID | Issue | Évidence | Chantier |
|----|-------|----------|----------|
| P1-1 | `PorceletIndividuel` type TS sans `logeId` (DB l'a) | `src/types/farm.ts:174-182` | A |
| P1-2 | Login bloqué pour compte audit (`invalid_credentials`) | Réponse Supabase HTTP 400 | (résolution user) |
| P1-3 | Prompt injection `marius-chat` via `context` non borné | `marius-chat/index.ts:94-96` | B |
| P1-4 | `send-push` Edge Function sans scope farm | `send-push/index.ts:112-125` | B |
| P1-5 | `signOut` ne vide pas `kvStore` (rôle persiste post-logout sur device partagé) | `AuthContext.tsx:208` | B |
| P1-6 | 5 SECURITY DEFINER functions exposées via `/rest/v1/rpc` | Supabase advisor + `is_member_with_role`, etc. | B/C |
| P1-7 | 91 lints performance Supabase (RLS initplan, FK non indexées, etc.) | Advisor performance | C |
| P1-8 | `api.porctrack.tech/health` retourne 404 (sous-domaine inutilisé) | curl + Playwright probe | (clarification user) |

### P2 (Mineur — confort / dette légère)

| ID | Issue | Évidence | Chantier |
|----|-------|----------|----------|
| P2-1 | Labels bottom-nav "Repro", "Performance" peu clairs pour éleveur africain | `BottomNav.tsx:24-34` | C |
| P2-2 | 149 warnings ESLint (46 Date.now purity, 35 react-refresh, 35 no-uuid-jsx) | `npx eslint src scripts` | C |
| P2-3 | 48 fichiers > 500L (supabaseWrites 1728L, TruieDetailView 1556L, etc.) | `find src \| xargs wc -l \| awk '$1>500'` | C |
| P2-4 | CSP prod `'upgrade-insecure-requests'` seul (CSP du nginx.conf jamais servie) | curl -I + nginx.conf vs Hostinger hcdn | B |
| P2-5 | `nginx.conf` repo destiné à VPS différent — inutilisé en prod | nginx.conf:6 (`app.porctrack.tech` ≠ `porctrack.tech`) | doc |
| P2-6 | `admin_logs.user_id` text sans FK auth.users | `20260508_rls_quickwins.sql:122-124` | C |
| P2-7 | CORS `*` sur Edge Functions | `marius-chat:47-51`, `send-push:64` | B |
| P2-8 | MIME validation upload photo basée sur déclaration browser | `photoUpload.ts:211` | C |

---

## Métadonnées audit

- **Date** : 2026-05-17
- **Orchestrateur** : Claude Opus 4.7 (1M context)
- **Sub-agents dispatchés** : 3 (qa-runner, code-reviewer, security-reviewer) — chacun a rencontré le sandbox Bash documenté `.claude/memory/blockers.md` 🟡, contourné par Read/Grep ou exécution orchestrateur
- **Spot-checks orchestrateur** :
  - ✅ `listLogesEffectivesParBande` existe (faux négatif code-reviewer corrigé)
  - ✅ Clé Mistral leakée DANS dist (confirmé F-01 CRITICAL)
  - ✅ `nginx.conf` repo ≠ config Hostinger prod
- **Screenshots** : `/tmp/audit-2026-05-17/PUB_*.png` (15 routes publiques) + `report.json` + `report-public.json`
- **Pipeline final** : TSC 0 erreur · vitest 2145/2145 · ESLint 0 erreurs / 149 warnings · build OK 3.09s · PWA 112 entries / 5.78 MiB
