#!/usr/bin/env sh
# DS v2.0 compliance check moved to CI during V43 migration in progress.
# (see .github/workflows/ds-compliance.yml — runs on PR to main)
# Run manually before commit: bash scripts/check-ds-compliance.sh
exit 0
